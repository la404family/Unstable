#include "..\macros.hpp"

/*
    LL_fnc_task03a_addAction

    Description:
      Action verte supprimée — l'extraction se fait via l'action hélicoptère permanente.
      Les rappels d'extraction sont gérés par LL_fnc_task04.

    Locality:
      Client uniquement (hasInterface)
*/

if (!hasInterface) exitWith {};
